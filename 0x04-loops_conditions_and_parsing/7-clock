#!/usr/bin/env bash
# define new function display 10 times with until statement
count=0
hour=0

while [ $hour -le 12 ]
do
  count=1
  while [ $count -le 59 ]
  do
    echo $count
    case $count in
      "59")
        echo "Hour: $hour"
        ;;
    esac
    (( count++ ))
  done
  (( hour++))
done